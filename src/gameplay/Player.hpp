#pragma once

/// Stores player state.
struct Player {
    int level;
};

/// Stores a server player id.
struct PlayerId {
    int value;
};

/// Marks a player in incantation.
struct Incantating {};
